<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_above="@+id/button_open_login"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="36dp"
        android:background="@drawable/logo_no_black_cropped" />

    <Button
        android:id="@+id/button_open_login"
        style="@style/HugeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/button_open_register"
        android:layout_alignParentStart="true"
        android:text="@string/action_login" />

    <Button
        android:id="@+id/button_open_register"
        style="@style/HugeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:text="@string/action_register" />

</RelativeLayout>